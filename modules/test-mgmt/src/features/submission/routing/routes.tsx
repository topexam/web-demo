import { RouteObject } from 'react-router-dom';
import { TestSubmissionPage } from '../pages';

export const featureRoutes: RouteObject[] = [
  {
    index: true,
    element: <TestSubmissionPage />,
  },
];
